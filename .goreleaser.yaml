version: 2
project_name: usher
builds:
  - env: [CGO_ENABLED=0]
    goos: [darwin, linux, windows]
    goarch: [amd64, arm64]
    ldflags: ["-s -w -X main.version={{.Version}}"]
archives:
  - formats: [tar.gz]
    format_overrides:
      - goos: windows
        formats: [zip]
homebrew_casks:
  - repository:
      owner: theodorebeaupre-prog
      name: homebrew-tap
      token: "{{ .Env.HOMEBREW_TAP_GITHUB_TOKEN }}"
    homepage: https://github.com/theodorebeaupre-prog/usher
    description: "One command. The right AI coding agent. Every time."
    hooks:
      post:
        install: |
          if system_command("/usr/bin/xattr", args: ["-h"]).exit_status == 0
            system_command "/usr/bin/xattr", args: ["-dr", "com.apple.quarantine", "#{staged_path}/usher"]
          end
    # Flip to "auto" once the HOMEBREW_TAP_GITHUB_TOKEN repo secret exists
    # (PAT with repo scope on theodorebeaupre-prog/homebrew-tap) — without it
    # the cask push fails and takes the whole release down with it.
    skip_upload: true
changelog:
  sort: asc
